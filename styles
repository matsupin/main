@font-face {
    font-family: 'Founders Grotesk';
    src: url('assets/fonts/FoundersGrotesk-Bold.otf') format('truetype'),
    font-weight: bold;
    font-style: normal;
}

@font-face {
    font-family: 'Founders Grotesk';
    src: url('assets/fonts/FoundersGrotesk-Regular.otf') format('truetype'),
    font-weight: normal;
    font-style: normal;
}

/* Import Japanese fonts from Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@100..900&display=swap');
@import url('https://fonts.googleapis.com/css2?family=M+PLUS+1p&display=swap');

body {
    margin: 0;
    padding: 0;
    background: #fff;
}

.menu-container {
    width: 80%;
    max-width: 700px;
    margin: 40px auto;
    padding: 20px;
}

.menu-title {
    font-family: 'Founders Grotesk', sans-serif;
    font-weight: bold;
    text-align: center;
    font-size: 72px; /* Adjusted for larger size */
    margin: 20px 0;
}

.section-title {
    font-family: 'Founders Grotesk', sans-serif;
    font-weight: bold;
    font-size: 30px;
    border-bottom: 1px dotted #000;
    padding-bottom: 5px;
    margin: 40px 0 20px;
}

.dish-name {
    font-family: 'Founders Grotesk', sans-serif;
    font-weight: normal;
    font-size: 10px; /* Smaller than h2 */
    margin: 10px 0;
}

.dish-text {
    font-family: "M PLUS Rounded 1c", sans-serif;
    font-weight: 400;
    font-style: normal;
}

.dish-description {
    font-family: "Noto Sans JP", sans-serif;
    font-optical-sizing: auto;
    font-style: normal;
    font-size: 16px; /* Smaller than dish-name */
    margin: 5px 0 20px;
}

.jp-text {
    font-size: 14px; /* Smaller font size for Japanese text */
}

.comment {
    font-size: 0.8em; /* Smaller font size */
    margin-top: 20px; /* Adds some space from the edges */
}
